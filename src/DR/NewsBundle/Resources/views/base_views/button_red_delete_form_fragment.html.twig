{{ form_start(form) }}  
    
    <div class="row ">
    	<div class="col-sm-12 textcenter">
            {{ form_row(form.deleteall, {'attr': {'class': 'btn btn-danger'}}) }}
    	</div>
    </div>
    {{ form_errors(form) }}
    {{ form_rest(form) }}
{{ form_end(form) }}